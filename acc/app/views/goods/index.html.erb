<p id="notice"><%= notice %></p>
<div class="row">
            <div class="box">
                <div class="col-lg-12 intro-text text-center">
                    <h1>بيانات الأصناف</h1>

                <table border=2px style="text-align:center" class="table table-bordered table-striped active info" cellspacing="0" width="100%" >

                      <thead>
                        <tr>
                           <th>الحساب</th>
                          <th>ملاحظات</th>
                          <th>الوحدة</th>
                          <th>الصنف</th>
                        </tr>
                      </thead>

                      <tbody>
                        <% @goods.each do |good| %>
                          <tr>
                            <td><%= link_to 'عرض', good %></td>
                            <td><%= good.note %></td>
                            <td><%= good.unit %></td>                            
                            <td><%= good.name %></td>
                          </tr>
                        <% end %>
                      </tbody>
                    </table>
    </div>
  </div>
</div>